class AppConfig {
  // Note: On Android emulator, '10.0.2.2' maps to your host machine's localhost.
  // On iOS simulator or web, use 'localhost' or '127.0.0.1'.
  // For physical devices, use your computer's local IP address (e.g., '192.168.1.X').
  static const String apiBaseUrl = 'http://10.0.2.2:3000/api';
}

